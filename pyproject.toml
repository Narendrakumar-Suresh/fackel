[project]
name = "fackel"
version = "0.1.1"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "jax>=0.10.2",
    "safetensors>=0.8.0",
]

[dependency-groups]
dev = [
    "ruff>=0.15.22",
]
